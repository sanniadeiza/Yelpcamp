version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20 || nvm install 20 && nvm use 20
        - npm install
        - if [ ! -f src/aws-exports.js ]; then echo "const awsmobile = {}; export default awsmobile;" > src/aws-exports.js; fi
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
